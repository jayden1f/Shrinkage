<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="curtin.edu.shrinkage">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shrinkage">
        <activity
            android:name=".ScannerActivity2"
            android:exported="true" />
        <activity
            android:name=".ScannerActivity"
            android:exported="true" />
        <activity
            android:name=".ShrinkageActivity"
            android:exported="true" />
        <activity
            android:name=".DateChecksActivity"
            android:exported="true"
            android:label="@string/title_activity_date_checks"
            android:theme="@style/Theme.Shrinkage.NoActionBar" />
        <activity
            android:name=".NewShrinkageActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>